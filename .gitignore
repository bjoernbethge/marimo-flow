# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Python cache directories
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.pyc
*.pyo

# Virtual environments
.venv
.cache/
__marimo__/

# Environment files
.env

# marimo-flow user config (checked-in templates end with .example)
config.yaml
config.yml
marimo-flow.yaml
marimo-flow.yml

# IDE and editor configs (user-specific)
.idea/
.cursor/
.cursorrules
# .vscode/ - committed for project-wide MCP setup

# AI assistant configs (user-specific)
.claude/settings.local.json
.marimo-flow/
.serena/
*-agent.prompt.yml
*.prompt.yml

# Keep .claude/Skills/ tracked (project-specific skills)

# Model cache directories (large files, download on-demand)
models/
/models/

# MLflow data
data/mlflow/db/
data/mlflow/artifacts/
mlruns/
mlflow/
mlruns.db
mlflow.db
mlartifacts/
# MLflow experiment-id directories from the legacy "no artifact_location"
# fallback (./<exp_id>/<run_id>/...). The lead agent now pins artifacts to
# data/mlflow/artifacts/, but ignore stragglers from older runs.
/[0-9]*/

# Lightning / PyTorch training artifacts
lightning_logs/

# DuckDB files
*.duckdb
*.duckdb.wal
agents/main.duckdb
agents/skills.duckdb